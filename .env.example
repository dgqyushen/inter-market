# =============================================================================
# Inter-Market 环境变量配置
# =============================================================================
# 本地开发: 复制此文件为 .env 并填入真实值
# Cloudflare Pages: 在 Dashboard -> Settings -> Environment variables 中配置
# =============================================================================

# -----------------------------------------------------------------------------
# 前端配置 (需要 VITE_ 前缀)
# 这些变量会被打包到前端代码中，不要放敏感信息！
# Cloudflare Pages 配置时也需要包含 VITE_ 前缀
# -----------------------------------------------------------------------------

# 交易对配置 (JSON数组格式)
# 每个交易对显示第一个标的相对于第二个标的的比值
# 例如: "QQQ/GLD" 表示 QQQ 以 GLD 计价
VITE_TRADING_PAIRS=["QQQ/GLD","IBIT/GLD","IBIT/QQQ"]

# A股基准ETF配置 (JSON数组格式)
# symbol: Yahoo Finance股票代码 (上证.SS, 深证.SZ)
# name: 显示名称
VITE_CN_BENCHMARK_ETFS=[{"symbol":"510300.SS","name":"沪深300ETF"},{"symbol":"510050.SS","name":"上证50ETF"},{"symbol":"159949.SZ","name":"创业板50ETF"}]

# -----------------------------------------------------------------------------
# 本地开发专用配置 (仅用于本地调试)
# 这些带 VITE_ 前缀的变量仅在本地开发时使用
# 生产环境应使用不带 VITE_ 前缀的服务端变量 (见下方)
# ⚠️ 不要将这些变量提交到 Git！
# -----------------------------------------------------------------------------

# OpenAI 兼容 API 配置 (本地开发用)
VITE_AI_API_URL=https://api.openai.com/v1/chat/completions
VITE_AI_API_KEY=your-api-key-here
VITE_AI_MODEL=gpt-4o

# -----------------------------------------------------------------------------
# Cloudflare Pages 服务端配置 (不带 VITE_ 前缀)
# 这些变量只在 Cloudflare Functions 中使用，不会暴露给前端
# 在 Cloudflare Dashboard 中配置这些变量 (不是在这个文件)
# -----------------------------------------------------------------------------

# AI_API_URL=https://api.openai.com/v1/chat/completions
# AI_API_KEY=sk-your-secret-key
# AI_MODEL=gpt-4o
